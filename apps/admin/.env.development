# 开发环境 API 基础路径（配合 vite.config.ts：/api/* 代理到后端 /api/*）
VITE_API_BASE_URL=/api

# 本地联调后端地址（仅 `vite dev` / `vite preview` 生效，不会打进生产构建）。
# 需要临时覆盖时可新建 .env.development.local：
# DEV_PROXY_TARGET=http://你的后端地址
DEV_PROXY_TARGET=http://192.168.1.4:8088
