# 开发环境配置 /api 与本地开发代理地址只在开发时生效。
VITE_API_BASE_URL=/api

# 本地联调后端地址（仅 `vite dev` / `vite preview` 生效，不会打进生产构建）。
# 开发环境后端地址
DEV_PROXY_TARGET=http://192.168.1.4:8088

# 开发环境后端现场地址
# DEV_PROXY_TARGET=http://10.137.162.120:8088
