# 开发环境 API 基础路径（配合 vite.config.ts：/api/* 代理到后端 /api/*）
VITE_API_BASE_URL=/api

# 本地联调后端地址（花生壳等内网穿透临时域名，仅 `vite dev` / `vite preview` 生效，
# 不会打进生产构建）。域名失效时新建 .env.development.local 覆盖：
# DEV_PROXY_TARGET=http://你的后端地址
DEV_PROXY_TARGET=http://115111ob8gw23.vicp.fun:48041
