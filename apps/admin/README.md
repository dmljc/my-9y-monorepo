# admin

后台管理应用，端口 `1111`，API 代理至 `http://localhost:3000`。

```bash
pnpm admin:dev    # 仓库根目录
pnpm admin:build
pnpm lint         # 根目录 Biome
```

**编码约定（Cursor）：** [.cursor/rules/admin-frontend.mdc](../../.cursor/rules/admin-frontend.mdc)  
**参考页面：** `src/pages/Login/`
