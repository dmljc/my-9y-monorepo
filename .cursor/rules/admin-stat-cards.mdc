---
description: admin 顶部统计卡片布局与插图定位（告警列表等页复用）
globs: apps/admin/src/pages/**/*
alwaysApply: false
---

# admin 顶部统计卡片

适用于告警列表、点检台账等页顶部的 3 列统计卡片；**参考实现**：`apps/admin/src/pages/warning-list/index.module.css`。

## 卡片结构

```tsx
<div className={`${styles.summaryCard} ${styles.summaryCardBlue}`} style={{ backgroundImage: `url(${bg})` }}>
  <div className={styles.summaryCardTitle}>{title}</div>
  <div className={styles.summaryCardValue}>{value}</div>
  <img className={styles.summaryCardIllustration} src={image} alt="" draggable={false} />
</div>
```

## 关键样式

| 类名 | 要点 |
|------|------|
| `.summaryCards` | `grid-template-columns: repeat(3, 1fr); gap: 12px` |
| `.summaryCard` | `height: 148px; padding: 24px 0 0 20px; position: relative; overflow: hidden` |
| `.summaryCard` 背景装饰 | `background-position: right -24px center; background-size: auto 118%` |
| `.summaryCardIllustration` | 见下方插图定位 |

## 插图定位（已验证）

设计稿边距：上 10px、右 40px、下 38px。**不要用 `left` 与 `right` 同时约束**，否则插图会被拉成宽矩形并偏左。

```css
.summaryCardIllustration {
  position: absolute;
  top: 10px;
  right: 40px;
  height: calc(100% - 48px); /* 10 + 38 */
  width: auto;
  object-fit: contain;
  object-position: right center;
  pointer-events: none;
  user-select: none;
}
```

## 替换卡片图片时

1. 插图放 `apps/admin/src/assets/warning/`（或对应业务目录），圆形装饰背景与 3D 插图分开资源。
2. 蓝 / 绿 / 橙三色对应 `.summaryCardBlue` / `.summaryCardGreen` / `.summaryCardOrange` 标题竖条色：`#0984ff` / `#2acd7f` / `#fa8c16`。
3. 新页对齐 `warning-list` 的卡片 CSS，仅替换图片路径与文案，**不要**改插图定位为百分比宽度或同时设 `left`+`right`。

## 待对齐页面

（暂无）
