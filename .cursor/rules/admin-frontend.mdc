---
description: admin 应用前端页面与组件约定（React + Ant Design）
globs: apps/admin/**/*
alwaysApply: false
---

# admin 前端约定

适用于 `apps/admin` 下新增或修改的页面、布局、组件。

## 组件

- 使用 **箭头函数组件**：`const Xxx = () => { ... }`，文件末尾 `export default Xxx`；**不强制** `FC`。
- 有 props 时：`const Xxx = (props: XxxProps) => { ... }`，单独定义 `XxxProps` 接口。
- **页面组件名用功能名即可，不要加 `Page` 后缀**（如 `Login`、`DigitalTwin`，不要 `LoginPage`）；目录名与组件名一致：`pages/Login/`。
- 业务逻辑保持精简，复杂逻辑抽 hooks/utils。
- 使用 **Ant Design** 默认主题即可；无明确需求时不要加 `ConfigProvider theme` 覆盖色板。

## Ant Design

- API、语义以[官方文档](https://ant.design/docs/react/llms-cn)为准，勿臆造 props。
- 外网开发时可联网检索，或使用 Cursor @Docs / MCP；无需在仓库内维护 `docs/` 副本。
- 写样式、`:global(.ant-*)` 覆盖时参考组件语义说明（如 https://ant.design/llms-semantic-cn.md）。

### JSX 属性换行

**做什么**：单行能清晰表达的 JSX 属性尽量写在一行，减少无意义换行。

**为什么**：短属性拆成多行只增加纵向篇幅，降低扫读效率。

**✅ 推荐**

```tsx
<Form.Item
  name={[field.name, "deviceName"]}
  rules={[{ required: true, message: "请选择设备" }]}
>
  <Select showSearch={{ optionFilterProp: "label" }} placeholder="请选择设备" options={DEVICE_OPTIONS} />
</Form.Item>
```

**❌ 避免**

```tsx
<Form.Item
  name={[
    field.name,
    "deviceName",
  ]}
  rules={[
    {
      required: true,
      message: "请选择设备",
    },
  ]}
>
```

**边界**：多条 `rules`、长字符串、复杂 JSX 子树、超过可读宽度的 props 仍允许多行；Prettier 强制换行时以 Prettier 为准。

## 样式（默认）

复杂页面（表单、列表、多区块布局）采用 **CSS Modules**：

```
pages/Login/
  index.tsx
  index.module.css   # 布局、间距、:global(.ant-*) 覆盖、响应式
```

- `index.tsx` 中：`import styles from "./index.module.css"`，用 `className={styles.xxx}`。
- **最外层根类名与组件名一致**（组件 `Login` → `.login` / `styles.login`，`DigitalTwin` → `.digitalTwin`）；不要用泛名 `page`。
- 覆盖 Ant Design 内部类名时用 `:global(.ant-input)` 等，避免全局裸 CSS。
- **仅一两处简单布局**（如容器 `height: 100%`、`overflow: hidden`、`display: flex`）用内联 `style` 即可；**不必**为此单独建 `index.module.css`。
- 页面业务复杂（如嵌入第三方编辑器）≠ 必须用 CSS Modules；以 **admin 侧自定义样式量** 为准：无多类名、无 `:global(.ant-*)`、无响应式时，优先内联，不要整页塞进 `style.ts`。

## 样式（例外）

仅当页面极简（单层容器、无 Ant 内部样式覆盖）时，可参考 `apps/dashboard` 使用同目录 `style.ts` 导出 `CSSProperties`。

## 其它

- 路径别名：`@/` 指向 `apps/admin/src`。
- 路由懒加载写法与 `src/routers/index.tsx` 保持一致。
- 提交前在仓库根目录执行 `pnpm lint`。

## 示例（页面骨架）

```tsx
import styles from "./index.module.css";

const Login = () => {
  return <div className={styles.login}>...</div>;
};

export default Login;
```
