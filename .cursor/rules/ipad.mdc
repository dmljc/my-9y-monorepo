---
description: ipad 端专属约定（目录、横屏、cqw 禁止写死、contain 舞台、电脑端等比不变形不裁切、统计卡）
globs: apps/ipad/**/*
alwaysApply: false
---

# ipad 端约定

适用于 **`apps/ipad`**。组件形态、事件命名、CRUD、Form / Modal / Table、api/utils 分层、HTTP 错误处理等与 admin **共用**，见 `.cursor/rules/admin-frontend.mdc`；未在本文件覆盖的一律按 admin 写法执行。银河麒麟断网交付亦见该文件。

本文件只收 **ipad 专属**：目录落位、端差异、模块边距、蓝湖尺寸、目标设备、平板/电脑等比 contain 舞台（**电脑端不变形、不裁切、留白合理**）、**尺寸须用 cqw 随舞台缩放（禁止写死 px）**、统计卡落盘。

蓝湖切图：优先 **Android xhdpi（2800×1840）** 或等价 **2x** WebP；电脑与平板共用同一套资源。

## ipad 目录与文件管理

**做什么**：`apps/ipad` 的目录、文件命名与页面拆分与 `apps/admin` **保持同一套风格**——按功能建 `pages/<功能名>/`，页面入口 `index.tsx`，配套 `api.ts` / `interface.ts` / `utils.ts` / `index.module.css`（按需）；**不要**把业务页长期堆在扁平的 `src/components/`。

**为什么**：两应用同 monorepo、同技术栈（React + Ant Design）；目录与编码一致后，跨端改需求、复用约定与 Code Review 成本更低。

### 推荐目录

```
apps/ipad/src/
  pages/
    dashboard/           # 目录小写；组件名仍用 Dashboard，不要 DashboardPage
      index.tsx          # 箭头函数组件，export default
      index.module.css   # 复杂布局用 CSS Modules（默认）
      api.ts             # HTTP 薄封装（有接口时）
      interface.ts       # 请求/响应类型（中文 JSDoc）
      utils.ts           # 纯函数；非必需不建
      CreateModal.tsx    # 独立交互弹窗才拆
    warning-list/        # 多词功能用 kebab-case，与 admin 一致
      index.tsx
      index.module.css
      ...
  layout/                # 有壳层布局时再建
  routers/               # 路由懒加载写法对齐 admin `src/routers`
  utils/                 # 跨页 request、工具
  assets/                # 静态图（优先 .webp）；统计卡插图按业务分子目录
  App.tsx
  main.tsx
```

### 与当前脚手架对齐时

| 现状（避免长期保留） | 目标 |
|----------------------|------|
| `src/components/Dashboard.tsx` + `style.ts` | `src/pages/dashboard/index.tsx` +（默认）`index.module.css` |
| `import type { FC }` + `const Xxx: FC` | `const Xxx = () => { ... }`，**不强制** `FC` |
| 业务组件散落 `components/` | 仅跨页复用的纯 UI 才进 `src/components/`；页面专属放对应 `pages/` |

### 编码要点（与 admin 同规，ipad 须遵守）

- 箭头函数组件、props 单独 `XxxProps`、页面名不加 `Page` 后缀。
- 事件：`handle` + 动作（本页 UI）；Form / Modal 等与 prop 同名用 `onFinish` / `onOk`。
- 列表 CRUD、搜索区、分页（默认 `pageSize` 15）、`loading` 命名、api 薄封装、utils 纯函数、HTTP 错误靠全局 `onError`——全文各节对 ipad **同等生效**。
- 有顶部 3 列统计卡时，结构与 CSS 对齐 `admin-frontend.mdc`「顶部统计卡片」；插图放 `apps/ipad/src/assets/<业务>/`。
- 路径别名：`@/` → `apps/ipad/src`（admin 为 `apps/admin/src`）。

### ipad 端差异（仅下列例外）

| 项 | 约定 |
|----|------|
| 产品形态 | 面向平板 / 触控大屏；全屏页、手势区可按设计加触摸友好间距，**不**因此另起一套目录或组件命名 |
| 方向 | **仅横屏**；布局、CSS、设计稿对齐一律按横屏视口，**不要**为竖屏写第二套布局或 `@media (orientation: portrait)` |
| `@media` | 设计稿**明确**要求平板断点时，可在该页 CSS Modules 写媒体查询；无标注则仍默认不写（见「不写响应式 @media」）。**不要**为竖屏或窄屏手机断点适配 |
| `style.ts` | 仅**极简**单层容器（无 Ant 内部覆盖、无多类名）时可用同目录 `style.ts` 导出 `CSSProperties`；复杂页一律 CSS Modules |
| 模块边距 | **统一逻辑 16**（`calc(16 / 1400 * 100cqw)`，见下节「ipad 模块边距」）；**不要**套用 admin「模块间 24px」 |
| CSS 尺寸 | 蓝湖 **1400×920** 逻辑标注；**可见尺寸一律** `calc(n / 1400 * 100cqw)`，**禁止**写死 `px` 字号/控件高（见「ipad CSS 尺寸与蓝湖落地」） |

### ipad 模块边距

**做什么**：`apps/ipad` 多模块页面（顶栏与内容区、左右白底面板、内容区与页边等）的模块间距与页面外边距统一为 **16**（蓝湖自定义画布 **1400×920** 时的逻辑像素）；写 `gap` / `padding` / `margin` 时用 `calc(16 / 1400 * 100cqw)`，与舞台同步缩放；**不要**自创 20 / 24 / 28 等间距，也**不要**套用 admin「模块间 24px」。

**为什么**：设备控制等横屏业务页设计稿已按 16 模块间距对齐；电脑端 contain 舞台缩小后，写死 `16px` 会相对稿面偏大，与其它 `cqw` 元素脱节。

**数值约定**

| 场景 | 数值 | 说明 |
|------|------|------|
| 页面四边留白（主内容相对视口） | **16** | 写成 `calc(16 / 1400 * 100cqw)` |
| 顶栏与主内容区间距 | **16** | 顶栏下方到侧栏/详情区 |
| 页面内独立模块之间（左列表 ↔ 右详情等） | **16** | 如 `.body { gap: calc(16 / 1400 * 100cqw) }` |
| 蓝湖标注基准 | 自定义画布 **1400×920** | 标注值即 CSS 逻辑像素；物理稿 2800×1840 为 2x，**不要**用 2800/1840 当 calc 分母 |

**✅ 推荐**

```css
/* 设备控制等多模块横屏页 */
.body {
  display: flex;
  gap: calc(16 / 1400 * 100cqw);
  padding: calc(16 / 1400 * 100cqw);
}
```

**❌ 避免**

```css
/* 不要写死 px，也不要把 admin 的 24 模块间距套到 ipad */
.body {
  gap: 16px;
  padding: 24px;
}
```

**边界**：登录 / 首页全屏背景等设计稿明确无模块 gutter 的页面除外；模块**内部**控件间距（图标与文案、Tab 间距等）仍跟单页蓝湖标注，同样优先 `calc(n / 1400 * 100cqw)`。admin 列表页仍见「页面边距」（模块间 24px / 搜索区 16px）。

### ipad CSS 尺寸与蓝湖落地（calc / cqw，禁止写死）

**做什么**：`apps/ipad` 从蓝湖还原样式时，以自定义画布 **1400×920** 的标注为唯一逻辑像素基准；页面落在 contain **舞台**内后，凡会影响视觉比例的尺寸（宽高、字号、行高、内边距、外边距、圆角、控件高度等）**一律**写 `calc(n / 1400 * 100cqw)`，与舞台 `cqw` **同步缩放**；**禁止**对上述属性写死 `px`（如 `font-size: 16px`）。**同一页不要混用** 1400 / 2800 / 1840 三套分母，也**不要**宽用 `cqw`、高用 `cqh` 拆开算（会把卡片比例拉歪）。

**为什么**：现场 MatePad 横屏与电脑浏览器共用一套样式。电脑端舞台 contain 缩小后，若布局/标题已用 `cqw`、而 Input / Select / 占位符仍写死 `16px`，会出现「整页缩小了、框内文字偏大/不协调」；管道配置等页已踩坑。统一 `/ 1400 * 100cqw` 后，MatePad 与电脑端观感一致。

**蓝湖与设备对照**

| 项 | 值 |
|----|-----|
| 蓝湖自定义画布（标注） | **1400 × 920** → 即 CSS 逻辑像素 |
| 设计稿物理像素（2x） | 2800 × 1840（仅切图/验收参考，**不作** calc 分母） |
| 现场机逻辑参考 | MatePad 横屏 3:2，与 1400×920 同比例 |

**写法分档**

| 场景 | 写法 | 示例 |
|------|------|------|
| 宽 / 高 / padding / margin / gap / 圆角 / 字号 / 行高 / 控件高度 | **必须** `calc(n / 1400 * 100cqw)` | `font-size: calc(16 / 1400 * 100cqw)` |
| Ant Design Input / Select / 占位符 / 表格内编辑控件 | **必须** `cqw`（含覆盖 `:global(.ant-input)`、`.ant-select-selection-item` 等） | 见下方反例 |
| 舞台根、页面根（要用 `cqw` 时） | `container-type: size` | `.stage` / `.home` / `.pipelineConfig` |
| **仅允许**的写死 `px` | `1px` 发丝描边、`border: 1px solid` | 不要用 `px` 写字号或控件高 |

**硬性约束**

1. **分母只认 1400**：`calc(n / 1400 * 100cqw)`；**禁止**同页再写 `/ 2800`、`/ 1840` 当缩放基准。
2. **固定宽高组件宽高同基准**：宽、高都用 `/ 1400 * 100cqw`，保持稿面宽高比；**禁止** `width: …100cqw` + `height: …100cqh`。
3. **禁止写死视觉尺寸**：`font-size` / `line-height` / `height`（控件）/ `padding` / `border-radius` 等**不要**写 `16px`、`19px`、`24px` 这类绝对像素；蓝湖标注 `n` 对应 `calc(n / 1400 * 100cqw)`。
4. **Ant 控件文本一并覆盖**：表格内 Input、Select 的选中项与 placeholder、弹窗 Form 内控件，须在页面 CSS Modules 用 `:global(.ant-*)` 把字号改成 `cqw`；**不要**依赖 Ant 默认 `px` 字号。
5. **不做多端适配**：不为竖屏、手机断点写第二套布局；验收以 MatePad 横屏 + 电脑宽屏（舞台 contain）为准。
6. **模块间距仍为逻辑 16**：见「ipad 模块边距」，写成 `calc(16 / 1400 * 100cqw)`。

**✅ 推荐**

```css
.pageRoot {
  width: 100%;
  height: 100%;
  container-type: size;
}

/* 蓝湖 1400×920 标注 137×172 */
.metricCard {
  width: calc(137 / 1400 * 100cqw);
  height: calc(172 / 1400 * 100cqw);
  border-radius: calc(10 / 1400 * 100cqw);
  font-size: calc(24 / 1400 * 100cqw);
}

/* 表格内 Input / Select：与舞台同步缩放 */
.pipeFieldInput:global(.ant-input) {
  height: calc(36 / 1400 * 100cqw);
  font-size: calc(16 / 1400 * 100cqw);
  line-height: calc(22 / 1400 * 100cqw);
}

.pipeFieldSelect:global(.ant-select) :global(.ant-select-selection-item) {
  font-size: calc(16 / 1400 * 100cqw);
}
```

**❌ 避免**

```css
/* 混用 2x 物理分母 */
.gap { gap: calc(20 / 2800 * 100cqw); }

/* 宽高拆 cqw / cqh → 非 1400:920 视口时比例被拉歪 */
.card {
  width: calc(137 / 1400 * 100cqw);
  height: calc(172 / 920 * 100cqh);
}

/* 舞台缩小后字号不跟缩放 → 电脑端输入框文字相对偏大 */
.pipeFieldInput:global(.ant-input) {
  font-size: 16px;
}
.form :global(.ant-select) {
  font-size: 16px;
}
```

**边界**：`1px` 发丝线可写死；颜色、`font-weight`、`font-family` 不涉及长度单位，不受本条约束。首页等历史页若仍含 `/ 2800`、`/ 1840` 或控件 `font-size: Npx`，**改动该页时一并**迁到 `/ 1400 * 100cqw`。

### ipad 目标设备与视口（布局 / CSS 兼容基准）

**做什么**：`apps/ipad` 的页面布局、全屏页尺寸、固定宽高、背景图、canvas / 图表容器等，以现场平板 **HUAWEI MatePad 11.5″ S（柔光版）横屏** 为兼容基准；开发与验收时优先保证该机横屏下无溢出、无关键裁切、触控可点。

**为什么**：ipad 端只部署在指定平板横屏使用，不以手机竖屏或多机型自适应为目标；把型号与分辨率写进约定后，CSS 与布局不会误按桌面宽屏或竖屏平板去适配。

**设备信息（现场机）**

| 项 | 值 |
|----|-----|
| 品牌 / 型号名 | HUAWEI MatePad 11.5″ S（柔光版） |
| 型号（型号） | `SLG-W10` |
| 屏幕尺寸 | 11.5 英寸 |
| 分辨率（物理像素） | **2800 × 1840** |
| 宽高比 | **3 : 2** |
| 刷新率 | 144Hz（布局一般无需特殊处理） |
| 内存 / 存储 | 12GB + 512GB（性能参考，不影响 CSS） |
| 颜色 | 深空灰 |

**横屏视口与 CSS 写法要点**

| 部分 | 要求 |
|------|------|
| 方向 | **只做横屏**：逻辑宽 > 逻辑高；设计稿、全屏页、`100vw` / `100vh`、固定定位均按横屏 |
| 不写竖屏 | **不要** `orientation: portrait`、竖屏专用断点，或「竖屏再换一列」布局 |
| 比例 | 按 **3:2**（横屏约 `2800:1840`）理解画面；全屏背景、封面图、地图容器优先 `object-fit` / 等比铺满，避免按 16:9 硬裁 |
| 物理分辨率参考 | 验收与切图可对照 **2800 × 1840**；样式落地按 **1400×920 逻辑标注**（见「ipad CSS 尺寸与蓝湖落地」），**不要**把布局写成死绑物理像素的绝对坐标 |
| 触控 | 按钮、列表行、热区保持可点尺寸；模块间距统一逻辑 **16**（`calc(16 / 1400 * 100cqw)`，见「ipad 模块边距」），**不要**套用 admin 的 24px |
| 与 admin 差异 | admin 面向桌面宽屏；ipad **不要**复制 admin 的窄屏 `@media`，也**不要**假设侧栏 + 大工作区同时存在 |
| CSS 缩放 | 关键页用 `calc(n / 1400 * 100cqw)` + 根节点 / 舞台 `container-type: size`；**禁止**字号与控件高度写死 `px`；**不要**为多机型再写一套适配 |

**✅ 推荐**

```css
/* 全屏横屏页：按视口铺满，不按竖屏折叠 */
.dashboard {
  width: 100%;
  height: 100%;
  overflow: hidden;
}
```

**❌ 避免**

```css
/* 不要为竖屏再写一套 */
@media (orientation: portrait) {
  .dashboard {
    flex-direction: column;
  }
}
```

**边界**：若日后更换其它平板，先更新本节设备表与比例，再改页面；临时用桌面浏览器调试时可模拟接近 **3:2 横屏** 的视口，但仍以真机横屏为准。

## 视口等比适配：1400×920 contain 舞台（全站强制）

适用于 **`apps/ipad` 下所有页面**（首页、登录、设备控制、管道配置、添加设备等，无一例外）。现场机为 **HUAWEI MatePad 横屏**；客户也会在 **电脑浏览器** 打开。电脑端须按下方「合理表现」做 **等比缩放**：相对 MatePad **不变形、不裁切**，允许四周留白，**不要**为铺满窗口而拉伸或裁切。

### 问题

只按容器宽度用 `100cqw` 换算宽高时：MatePad（≈3:2）正常；**电脑更宽更矮**时高度超出视口，底部被裁切。若改为强制铺满浏览器窗口，则会出现拉伸变形或裁切——均不符合交付要求。

### 电脑端适配 MatePad：合理表现（验收口径）

**目标**：电脑浏览器中的画面与 MatePad 横屏 **同一构图、同一比例**，仅整体缩放；观感一致，信息不丢失。

| 原则 | 含义 | 电脑端允许 / 不允许 |
|------|------|---------------------|
| **不变形** | 始终保持蓝湖 **1400:920**（与 MatePad 横屏 3:2 同比例） | ✅ 等比缩放；❌ 拉满窗口导致宽高比被拧歪 |
| **不裁切** | 顶栏、表格、按钮、弹窗等须完整可见 | ✅ 内容全部落在舞台内；❌ 底部/侧边被视口裁掉 |
| **等比缩放** | 舞台在视口内 **contain**，整页（含字号、控件）随舞台 `cqw` 同比缩小或放大 | ✅ 统一 `calc(n / 1400 * 100cqw)`；❌ 布局用 `cqw`、输入框字号写死 `px` |
| **留白合理** | 视口比例 ≠ 1400:920 时，舞台居中，**外侧出现灰/底色留白** | ✅ **视为正确表现，不是缺陷**；❌ 强行 `width/height: 100%` 铺满浏览器以消灭留白 |

**各视口下的预期观感**

| 视口 | 合理表现 |
|------|----------|
| ≈3:2（MatePad 横屏） | 舞台铺满可用区域，基本无留白 |
| 更宽（常见电脑宽屏） | 按高度缩放，**左右留白**，内容完整、不变形 |
| 更高（偏方/偏竖的调试窗） | 按宽度缩放，**上下留白**，内容完整、不变形 |

**一句话**：电脑端「没有铺满浏览器窗口、四周有底色边」——在比例与 MatePad 不一致时 **是 contain 的正常结果**；验收看 **是否等比、是否裁切、字号是否跟舞台一起缩**，不看是否铺满窗口。

### 强制做法：每页（或统一壳层）必须有 contain 舞台

外层铺满视口并居中；内层 `.stage` 固定 **1400:920**，在视口内 **contain**。舞台内再设 `container-type: size`，页面构图与 **全部** `calc(n / 1400 * 100cqw)`（含 Input / Select 字号）写在舞台内，与舞台同步缩放。

**默认采用 contain 效果**：保证「完整显示、等比不变形」；**不要**为消除电脑端留白而改成 cover / 拉满视口（会裁切或变形）。

**优先**：在 `apps/ipad/src/layout`（或路由壳）做一次舞台，子页面只填内容，避免每页重复。若某页独立全屏（如登录、首页无 layout），则该页自己套舞台；即使登录页内部对齐/间距有差异，缩放逻辑仍必须是 `contain`（不得裁切/不得不等比）。

**参考实现**：`apps/ipad/src/pages/home/index.tsx` + `index.module.css`；业务页示例 `apps/ipad/src/pages/pipeline-config/`。

```tsx
<div className={styles.page}>
  <div className={styles.stage}>{/* 本页全部 UI */}</div>
</div>
```

```css
.page {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: #f4f9ff; /* 留白区同页底色；电脑端四周可见属正常 */
  container-type: size;
}

.stage {
  position: relative;
  width: min(100cqw, calc(100cqh * 1400 / 920));
  height: min(100cqh, calc(100cqw * 920 / 1400));
  overflow: hidden;
  background: #f4f9ff;
  container-type: size;
}
```

### 约束（硬性）

- **所有页面**都必须落在 contain 舞台内；**禁止**以「列表页 / 业务页不是整页插画」为由跳过。
- **不要**为电脑端写第二套 `@media` 或竖屏布局；只做横屏 + 等比 contain。
- **不要**为「铺满浏览器」而去掉 contain、改为 cover，或单独拉宽/拉高舞台（必然变形或裁切）。
- **不要**在舞台外用裸 `100vw` / `100cqw` 做关键页绝对定位或大块高度（易再次裁切）。
- 舞台内视觉尺寸（含 Input / Select **字号**）须用 `calc(n / 1400 * 100cqw)`，**禁止**写死 `font-size: Npx` 导致电脑端「框随舞台缩小、字不缩」；见「ipad CSS 尺寸与蓝湖落地」。
- 模块间距逻辑值为 **16**，写成 `calc(16 / 1400 * 100cqw)`；见上文「模块边距」。
- 新增或改版页面时：先确认舞台（壳层或本页），再写 `cqw` 样式；验收须同时看 MatePad 横屏与电脑宽屏——电脑端 **允许留白**，**不得裁切、不得变形**，控件文字须与整页同比缩放。

### 电脑端裁切、变形与字号不同步的高风险点（务必避免）

当电脑端宽屏导致高度更矮时，若内部仍用真实视口高度会在缩放后超出舞台被裁切；若强制铺满窗口会变形；若字号写死 `px`，会出现整页缩小但输入框文字不缩。

1. **禁止**在 `apps/ipad` 的页面/组件布局中直接使用：
   - `height: 100vh` / `height: 100dvh`
   - `min-height: 100vh` / `min-height: 100dvh`
   - `style={{ minHeight: "100vh" }}` 等等价写法
2. **禁止**在舞台内对可见字号/控件高度写死 `px`（如 `.ant-input { font-size: 16px }`），须改为 `calc(n / 1400 * 100cqw)`。
3. **禁止**用「宽 100% + 高 100% 且打乱 1400:920」的方式消灭电脑端留白。
4. 在舞台内的布局优先使用：
   - `height: 100%`（由舞台决定实际像素）
   - flex 布局时补 `min-height: 0`（避免子容器撑开导致溢出裁切）

### 壳层建议（优先）

为避免“舞台不生效/页面还在按真实视口算高度”，建议把 contain 舞台放到统一壳层：

- `apps/ipad/src/layout/index.tsx`：在壳层中把 `Outlet` 包在 `.stage` 内（与 home 的舞台实现一致）
- `apps/ipad/src/layout/index.module.css`：外层容器用 `height: 100%`（不要再依赖 `100dvh` 来决定内容高度）
- 全局 loading/兜底（例如 `RouteLoading`）避免 `minHeight: 100vh`，改用 `minHeight: 100%` 或继承舞台高度

如果某页必须独立全屏（例如不进入壳层或专用壳），才允许使用 `100vh/100dvh`，但需明确该页不在 contain 舞台内。

## 顶部统计卡片（ipad）

卡片结构、类名与插图定位与 admin **共用**，见 `.cursor/rules/admin-frontend.mdc`「顶部统计卡片」（参考 `apps/admin/src/pages/warning-list/index.module.css`）。

**ipad 侧补充**：

| 项 | 约定 |
|----|------|
| 插图落盘 | `apps/ipad/src/assets/<业务>/`（优先 `.webp`） |
| 尺寸 | 若该页关键构图走 `cqw`，统计卡宽高/字号用 `calc(n / 1400 * 100cqw)`，**不要**另起一套类名 |
| 布局 | 统计卡须落在 contain 舞台内，避免电脑端裁切 |
